# 黄裳行旅散文集 · 重编项目（Huang Shang Travel Anthology）

一个面向「文学研究 + 专业出版 + 数字人文」的开放工作项目，目标是**重新搜集、整理、编排黄裳（1919–2012，原名容鼎昌）的散文与游记**，据此设计一部新的《黄裳行旅散文集》，使其选目、结构、注释与编排水准超越目前市面已有的同类选本。

> 本项目目前处于**框架搭建阶段**：已建立可扩展的数据结构、编辑原则、目录方案与研究工作流，并录入了一批已核 / 待核篇目。所有结论均标注「确定 / 待核 / 推测」。**未查证者一律标待考，不编造篇名、出处或年份。**

---

## 一、核心判断

黄裳的游记本质上不是普通旅游散文，而是**「地点触发的文史随笔」**：风景、旧迹、掌故、书籍、戏曲、人物与历史感交织在一起。全书结构据此建立——不按原书名或出版年代堆砌，而按**时间、地点、场景、主题、文史脉络**重新分门别类。

## 二、目标

1. 建立结构化、可持续补充的**篇目索引数据库**（地点 / 时间 / 场景 / 主题多维标签）。
2. 设计一部**八辑结构**的新选本目录（见 `outline/`）。
3. 形成可操作的**选编原则、导读框架、注释框架与出版方案**。
4. 输出专业总编辑级别的判断：必收 / 可选 / 不宜收 / 待核 / 选本独特性。

## 三、版权边界（重要）

- 黄裳作品**仍在版权保护期内**（作者2012年逝世）。本项目**不大段复制受保护全文**。
- 本仓库存储的是：**篇目索引、版本来源、内容摘要（≤200字，原创转述）、主题/地点标签、时间线、辑目结构、导读与注释框架、出版方案**。
- 必要引用**只保留短句**并记录出处；网络流传的节选不作为可靠底本。
- 数据库中每条记录均带 `copyright_status_note` 字段提示使用注意。

## 四、文件结构

```
huangshang-travel-anthology/
├── README.md                        # 本说明
├── data/
│   ├── huangshang_essays.json       # 结构化篇目表（单一事实来源 / source of truth）
│   ├── huangshang_essays.csv        # 由 JSON 派生的表格版（供 Excel/校对）
│   ├── build_csv.py                 # 由 JSON 生成 CSV 的脚本（改 JSON 后重跑）
│   ├── validate.py                  # 数据校验脚本（id唯一、受控词表、字段完整性）
│   └── build_timeline.py            # 由 JSON 生成行旅年表 Markdown（输出至 notes/）
├── notes/
│   ├── source_bibliography.md       # 资料来源与版本目录
│   ├── editorial_principles.md      # 选编原则
│   └── timeline_draft.md            # 行旅年表（由 build_timeline.py 生成，勿手工修改）
├── outline/
│   ├── new_anthology_outline.md     # 新文集八辑目录方案
│   ├── volume_preface_draft.md      # 总序草稿
│   └── section_introductions.md     # 每一辑导读草稿
└── workflow/
    └── research_workflow.md         # 可持续研究工作流（查篇 / 标注 / 时间线 / 去重 / 异文 / 成目）
```

## 五、数据字段

每条篇目记录（`data/huangshang_essays.json` → `essays[]`）含：

| 字段 | 含义 |
|---|---|
| `id` | 内部稳定编号 |
| `title` | 篇名 |
| `original_collection` | 原收入书名 |
| `approximate_year` | 写作/首次发表时间，不可考则空 |
| `place` | 主要地点 |
| `region` | 蜀中/西南/金陵/江南/北平/上海/海外 等 |
| `scene_type` | 城市/江河/古迹/书肆/剧场/战时现场/旧宅/旅馆/寺观 等 |
| `themes` | 历史兴亡/掌故/书话/戏曲/人物/战时经验/城市记忆/江南文化/故都余韵 等 |
| `summary` | ≤200字原创摘要（非原文复制） |
| `editorial_note` | 为何值得收入、放在哪一辑 |
| `proposed_section` | 拟入辑号 1–8（0 表示附录/序跋） |
| `source_reference` | 版本或资料来源 |
| `copyright_status_note` | 版权与使用注意 |
| `confidence` | 确定 / 待核 / 推测 |

## 六、工作方法

1. **JSON 为唯一事实来源**，CSV 由 `build_csv.py` 派生，避免双份手工维护。
2. 新增/修改篇目 → 编辑 JSON → 运行 `python data/build_csv.py`。
3. 任何写入数据库的事实都要带 `confidence` 与 `source_reference`；查不到的标 `待核`，绝不臆造。
4. 以**《黄裳集》（山东人民出版社）**为主要底本与核对基准，辅以图书馆书目核定原始出版信息。

## 七、当前状态速览

- 已录入篇目：**58 条**，八辑均有已核/待核篇目；另收录 9 部书锚记、5 种现有对照选本、3 类「不宜收」判例。
- 每篇带 `inclusion` 分级（必收 / 可选 / 资料库）。第二辑《关于美国兵》15篇已做精选：5必收＋4可选＋6资料库（不整辑照收）。
- 覆盖较好：第三辑（金陵，**24篇**，《金陵五记》全五个时间层基本录完）、第二辑（《关于美国兵》完整15篇）、第一辑（《锦帆集》蜀道/西南诸篇，**9篇**）、第八辑（人物，**5篇**）。
- 仍偏弱：第四辑（江南，3篇）、第五辑（北平，仅《琉璃厂》）、第六辑（古迹，0篇独立篇，天王府等与第三辑互见）、戏曲题材。**注：补这几辑需逐篇目录，而《花步集》《晚春的行旅》《翠墨集》的目录页在豆瓣/中图网等站被出口代理按策略拦截（403），须改走图书馆 OPAC 或原书。**
- 数据库另设 `existing_selections`（对照选本）、`not_recommended`（不宜收判例）、`needs_verification`（待核清单）、`gaps`（缺口）四块。
- 主要缺口见 `data/huangshang_essays.json` 的 `gaps[]`/`needs_verification[]` 与 `notes/source_bibliography.md`。
- `data/timeline_draft.md`（由 `build_timeline.py` 生成）可供总序附录「黄裳行旅年表」参考。

> **重要更正**：旧版把《白门秋柳》当作1942年原始结集；经核，豆瓣所见《白门秋柳》是**后出主题选本**，单篇《白门秋柳》初出《锦帆集》。详见 `notes/source_bibliography.md`。

## 八、引用与署名

研究性整理项目，仅作学术与出版策划用途。正式出版前须取得版权方授权，并以原书核校全部篇目、年份与文字。
